<button type="default" size="{{defaultSize}}" loading="{{loading}}" plain="{{plain}}"
        disabled="{{disabled}}" bindtap="toHome" hover-class="other-button-hover"> 回认证页 </button>
        <text> \n </text><text> \n </text>
<button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
        disabled="{{disabled}}" bindtap="toReservation"> 预约 </button>
<button type="warn" size="{{warnSize}}" loading="{{loading}}" plain="{{plain}}"
        disabled="{{disabled}}" bindtap="toModify"> 变更 </button>

<text> \n </text><text> \n </text><text> \n </text><text> \n </text>
